# Single source of truth for the LOCAL SurveyJS V3 build location.
# See .env.example for details. Override per-machine in .env.local.
# After changing this, run:  npm run sync:deps && npm install
SURVEYJS_LIBV3=C:/survey.js/LibV3

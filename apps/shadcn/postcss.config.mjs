/** Tailwind CSS v4 for Next.js App Router via the PostCSS plugin.
 *  (The canonical demo uses the Vite plugin; Next uses @tailwindcss/postcss.) */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
